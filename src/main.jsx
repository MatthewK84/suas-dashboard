import React from "react";
import ReactDOM from "react-dom/client";
import SUASDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SUASDashboard />
  </React.StrictMode>
);
